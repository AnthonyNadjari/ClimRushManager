# URL publique du site (sans slash final). Si défini, chaque QR machine pointe vers ${NEXT_PUBLIC_APP_URL}/m/[id].
# Sinon le QR encode le texte CLIMRUSH|M|[id] (toujours lisible par le scanner intégré).
NEXT_PUBLIC_APP_URL=
